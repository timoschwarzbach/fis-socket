This is the project for the socket of the fis system.
It should run once per vehicle and controls the content displayed on the displays

dependencies:

-   ffmpeg (to get the duration of videos displayed)

run:
go run .
